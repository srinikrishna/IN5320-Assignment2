import React from 'react';
interface StaticInputOptions {
    warn?: boolean;
    name?: string;
}
export declare const useStaticInput: <T>(staticValue: T, { warn, name }?: StaticInputOptions) => [T, React.Dispatch<React.SetStateAction<T>>];
export {};
