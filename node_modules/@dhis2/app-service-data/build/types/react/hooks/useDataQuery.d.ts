import { QueryOptions } from '../../engine';
import { QueryRenderInput } from '../../types';
export declare const useDataQuery: (query: Record<string, import("../../engine").ResourceQuery>, { onComplete, onError, variables, lazy }?: QueryOptions) => QueryRenderInput;
