import { DataEngineLink } from '../engine';
export declare class ErrorLink implements DataEngineLink {
    private errorMessage;
    constructor(errorMessage: string);
    executeResourceQuery(): Promise<never>;
}
